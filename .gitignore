# Dependencies
node_modules/
*.tgz
pnpm-lock.yaml
bun.lock
deno.lock
pnpm-workspace.yaml

test-output/*
coverage/
__pycache__/
*.pyc

data

# Logs
logs/
*.log
npm-debug.log*

# Build output
build/*.txt

# Environment variables
.env

# System files
.DS_Store
Thumbs.db

# Development tools and configs
.prettierrc

# AI assistant files
CLAUDE.md
.ai/*
.idea
.gemini
.mcp.json
CLAUDE.local.md
.serena/
.claude/settings.local.json

z*/

_bmad
_bmad-output

# Module validation reports: local review artifacts, kept off the shipped surface
# (.npmignore already excludes them from the package). Matches 71bce7a.
skills/reports/

.clinerules
.augment
.crush
.cursor
.iflow
.opencode
.qwen
.rovodev
.kilocodemodes
.claude
.codex

# Astro / Documentation Build
website/.astro/
website/dist/
build/

# Playwright MCP session artifacts
.playwright-mcp/

# Skill quality-analysis artifacts and per-skill decision logs (local-only)
**/.analysis
**/.decision-log.md
**/.memlog.md
.claude/.story-automator-active

# Test fixtures are tracked even when they mimic a local-only artifact. A fixture
# that reproduces a run folder has to contain the same dot-prefixed files a real
# run writes, and the patterns above would otherwise swallow it: `git add` on an
# ignored path is a silent no-op, so the fixture stays behind, the suite passes
# locally, and CI fails on a clean checkout with nothing to read.
!skills/ultracode-goal/scripts/tests/fixtures/**/.decision-log.md
!skills/ultracode-goal/scripts/tests/fixtures/**/.memlog.md

# graphify knowledge-graph output (generated locally, not shipped)
graphify-out

# UCG
_ucg-learn

# IWE memory: per-machine session bookkeeping. The policy (MEMORY.md), the
# notes under memory/ and .iwe/config.toml + .iwe/schemas/ are tracked — iwe
# hides gitignored paths from the graph, so memory cannot live in ignored files.
/.iwe/claude/

# Agent-skill tooling: local sidecar memory, workspace artifacts and learning scaffold
_bmad/_memory/
/forge-data/
_skf-learn

# CocoIndex Code (ccc)
/.cocoindex_code/
