# Contributor Covenant (external standard)
CODE_OF_CONDUCT.md

# BMAD runtime folders (user-specific, not in repo)
_bmad/
_bmad*/

# Documentation website (Astro project with its own toolchain)
website/

# Build output
build/

# Temp reference files
temp/

# Dotfile directories (tool-specific runtime data)
.*

# Skill quality-analysis artifacts (local-only, gitignored)
skills/**/.analysis/

# Workflow-script dialect (top-level await + return) — not standard JS
skills/**/assets/*.workflow.js

# Test fixtures — hand-crafted artifact sets (some deliberately malformed) that
# must stay byte-exact for the fail-closed / deterministic kernel tests.
skills/**/scripts/tests/fixtures/
