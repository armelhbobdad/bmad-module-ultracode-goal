# PRD — Floor Fixture (all_clean)

Product requirements for the clean floor fixture. This document exists and is
readable so `prd_present` is true.

## FR-1
The system emits a JSON payload with exactly the documented keys.
