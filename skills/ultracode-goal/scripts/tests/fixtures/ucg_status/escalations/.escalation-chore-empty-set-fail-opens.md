# Turn budget escalation — story chore-empty-set-fail-opens

Story turn budget exceeded (turns 28/25). UltraCode-Goal stops this story and escalates: re-scope, split, or hand off. A Stop hook cannot interrupt a /goal condition mid-turn, so treat this as advisory — the deterministic guard is the gate_eval re-loop budget.
