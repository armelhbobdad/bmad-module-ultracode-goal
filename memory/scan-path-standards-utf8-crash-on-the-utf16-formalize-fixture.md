---
created: "2026-06-27 10:49"
session: "f27ba5af-8faf-452f-bc17-e10682899ad5"
---

# scan-path-standards.py UTF-8 crash on the UTF-16 formalize fixture

The BMad-installed `scan-path-standards.py` (`.claude/skills/bmad-workflow-builder/scripts/`, with a second copy under `bmad-agent-builder/scripts/`, both gitignored) reads every `*.md` and `*.json` under a skill with `read_text(encoding='utf-8')` unguarded, so over `skills/ultracode-goal` the stock copy dies with `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xff in position 0: invalid start byte` on the deliberate UTF-16 fixture `scripts/tests/fixtures/formalize/unreadable_epic/impl-artifacts/3-1-broken.md` (asserted on by `test_formalize_check.py`, so it will not change). The Analyze prepass captures stdout as `.analysis/<ts>/prepass/paths.json 2> paths.err`, so the crash leaves a zero-byte `paths.json` that reads exactly like a clean scan; reproduce with `python3 .claude/skills/bmad-agent-builder/scripts/scan-path-standards.py skills/ultracode-goal > /tmp/x.json 2> /tmp/x.err`. Even without the crash the stock scanner is about 99% noise on this skill (912 of 924 findings on `.analysis/`, `scripts/tests/` and fixtures). The locally patched workflow-builder copy (`except (UnicodeDecodeError, OSError)` plus an `excluded_dirs` set with `.analysis`, `tests`, `fixtures` and dot-directories) reports 12 findings, all on the gitignored `.decision-log.md` and `.memlog.md`, but any BMad reinstall wipes the patch (the 2026-07-12 reinstall did), so before trusting a clean lens result check that every `prepass/*.json` is non-empty valid JSON and `grep -n UnicodeDecodeError .claude/skills/bmad-workflow-builder/scripts/scan-path-standards.py`. The trap is the assistant's contemporaneous record (sessions f27ba5af and be035cf1).
