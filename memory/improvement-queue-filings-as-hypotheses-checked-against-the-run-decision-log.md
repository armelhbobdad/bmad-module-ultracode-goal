---
created: "2026-08-03 02:40"
session: "4e2f68a1-8a5e-438b-af3a-ea847b88489e"
---

# Improvement-queue filings as hypotheses checked against the run's decision log

A health-check filing in the ultracode-goal improvement queue is a hypothesis about a defect, not a diagnosis: before implementing its proposed fix, read the originating run's own `.decision-log.md` as ground truth and reproduce the symptom at the module version the reporting run used (the filing's Environment table carries a Module Version row; read a reference at that tag with `git show v2.0.0:skills/ultracode-goal/references/finalize.md`), not only on main. The user's rule, from the 2026-08-03 handoff prompt: "Treat each filing as a hypothesis, not a diagnosis: check the run's own decision log before implementing. fp-8f9d694 was misdiagnosed that way and the correct, smaller fix shipped as #81." That filing blamed a Stage 3 skip when the real gap was gate.md's non-web branch; on 2026-08-03 three of five filings verified were confirmed-but-misdiagnosed, and one proposed fix (prefix matching for `gate --story` slugs) would have reintroduced the epic-resolves-child fail-open that `skills/ultracode-goal/scripts/gate_trail.py` already documents. Expect a meaningful fraction of filings to need a different, smaller fix than the one proposed; the precedent is recorded only in commit 21996a0's body. The tag half is the assistant's practice from session aaa0183f: fp-158b543 claimed [finalize.md](../skills/ultracode-goal/references/finalize.md) never showed `gate_trail.py`'s invocation form, yet the block sat at line 40 of the v2.0.0 file, so the reporter had missed it and reading only main could not tell "already there" from "added since". A refuted filing is not written `record --action resolved` (a resolved record makes a re-sighting file as REGRESSION, a refuted one lets it suppress as a duplicate); commit 0ca1861 (#87) lists "refutes fp-158b543" apart from its "Closes" list and still carried a one-clause clarifying edit, so a refutation can carry a small edit without becoming a fix.
