---
created: "2026-06-27 21:07"
session: "c0f79f39-73b8-4df8-b105-ffa489da6b07"
---

# Tracked-tree ban on gitignored _bmad-output planning ids

Tracked files (README.md, docs/, skills/*/references/, tools/cli, code comments, docstrings, test comments, and anything an autonomous run writes) must not cite ids that resolve only inside the gitignored `_bmad-output/` planning corpus: `FR-n`, `NFR-n`, `AD-n`, `INV-n`, `OQ-n`, `R-n`, `AC-n` (write "Case N" in test sections), `Story N.N`, decision-log pointers such as `(D12)` or "deferred dN", and milestone names like "Epic-1 rule"; describe the behaviour instead. No user words are on record: the rule comes from the assistant's contemporaneous record of session c0f79f39 and is stated in commit 71bce7a (PR #21), where shipped code "had accumulated dangling traceability citations (AD-5, NFR-9, FR-5, INV-7, Story 2.x, OQ4, R4, decision-log dN, "Epic-1 rule") that point at those invisible docs", which are "noise to anyone who clones the repo". Not leakage: the detector regexes in `skills/ultracode-goal/scripts/formalize_check.py`, synthetic fixture content under `scripts/tests/fixtures/`, id-shape examples in `story_sizing.py` and `mutate_check.py`, and the `d1`/`d2` ledger row ids in [gate.md](../skills/ultracode-goal/references/gate.md), which are the tool's own output contract. Nothing in `npm run quality` scans for planning ids and CONTRIBUTING.md does not state the rule, so it is applied by hand, and the `AC-n` clause had already regressed at v2.2.0 in `test_finalize_graphify_refresh.py`, `test_on_escalation_hook.py` and `test_run_status_heartbeat.py` (`# --- AC-1: ... ---` markers and "AC-n twin" docstrings). Re-check with `git grep -nE '\b(FR|NFR|AD|INV|OQ)-?[0-9]+\b' -- ':!skills/ultracode-goal/scripts/tests/fixtures'`, expecting only the `FR-99` self-test vocabulary.
